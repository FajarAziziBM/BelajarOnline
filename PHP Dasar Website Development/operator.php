<?php
    // Operator Aritmatika
        // + , -, *, /, %, ** 

        // $a = 10;
        // $b = 5;
        // $total = $a + $b;
        
        // echo $total;

    // Operator Assigment
        // $a = 10;
        // $b = 5;

        // $total = 20;
        // // $total = $total + $a; atau  $total += $a;
        // $total += $a;
        // echo $total;
    
    // Operator Perbandingan
        // ==(equal), ===(identical), !=(not equal), <>(not equal), !==(not identical), >(greater than),
        // <(less than), >=(greater than or equal to), <=(less than or equal to), <=> (spaceship)

        // $a = 5;
        // $b = 3;
        // $komparasi = $a <=> $b;

        // echo var_dump($komparasi);

    // Operator Increment/decrement
        // $a = 1;
        // $b = 2;
        // $a++;
        // $b--;
        // echo $a;    
        // echo $b;    

    // Operator String
        $a = "Hello";
        $b = " Word";
        echo $a . $b;

?>